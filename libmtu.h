#pragma once
#include <stdarg.h>

// function names
void testfunc(); // help
int mtu_canDivide(int, int);
void mtu_qsort(float*, int);
char* mtu_lang();
int* mtu_pairSum(int, ...);
int mtu_printFiles();
